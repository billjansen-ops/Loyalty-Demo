# Learnings Process History

This folder holds older process-oriented artifacts that used to clutter the
top level of `learnings/`.

Subfolders:
- `startup/` — old start-chat instruction files
- `workflow/` — old workflow standard files
- `secret-sauce/` — historical model/session guidance notes
- `handoffs/` — older process-specific handoff helper notes

Use these only for historical context.
